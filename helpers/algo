OPENNESS

-- Strong correlation with ARTISTIC and moderate with INVESTIGATIVE
    - If Agreeableness is high, the person is ARTISTIC
    - Else, the person is INVESTIGATIVE

EXTRAVERSION

-- Strong correlation with ENTERPRISING and moderate with SOCIAL
    - If AGREEABLENESS is high, the person is SOCIAL
    - Else, the person is ENTERPRISING

AGREEABLENESS

-- Moderate correlation with SOCIAL

CONSCIENTIOUSNESS
-- Moderate correlation with CONVENTIONAL

// Remove all traits with percentage values between 32% and 68%, as it's inside the common people.



Get the personality object, get the trait values into an array, sort the array (highest to lowest), get the first two elements.